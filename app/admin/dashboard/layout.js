
// AdminDashboardLayout: Uses the main app layout's header, sidebar, and footer. No duplication.
// All admin dashboard pages use the unified layout and navigation.
export default function AdminDashboardLayout({ children }) {
  return (
    <>{children}</>
  );
}
